@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "نسيت كلمة المرور؟";
}

<div class="d-flex justify-content-center align-items-center" style="min-height: 70vh;">
    <div class="card shadow p-4" style="max-width: 450px; width: 100%; border-radius: 15px;">
        <div class="card-header text-center text-white" style="background: linear-gradient(135deg, #3498db, #2c3e50); border-radius: 15px 15px 0 0;">
            <h2>@ViewData["Title"]</h2>
        </div>

        <div class="card-body">
            <p class="text-center text-muted mb-4">
                أدخل البريد الإلكتروني أو رقم الواتساب وسنرسل لك خطوات إعادة تعيين كلمة المرور.
            </p>

            <form method="post">
                <div asp-validation-summary="ModelOnly" class="text-danger text-center mb-3" role="alert"></div>
                <div class="mb-3">
                    <label asp-for="Input.Identifier" class="form-label fw-bold"></label>
                    <input asp-for="Input.Identifier" class="form-control" autocomplete="username" aria-required="true" placeholder="name@example.com أو 07XXXXXXXXX" />
                    <span asp-validation-for="Input.Identifier" class="text-danger d-block text-center mt-2"></span>
                </div>
                <button type="submit" class="btn btn-primary w-100" style="background: linear-gradient(135deg, #3498db, #2c3e50); font-weight: 600;">
                    إرسال تعليمات الاستعادة
                </button>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
